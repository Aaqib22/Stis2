<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.bluecodic.stis">

    <application
        android:name="android.support.multidex.MultiDexApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_stis"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_stis"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".First"/>


        <activity
            android:name=".navigation_bar"
            android:label="@string/title_activity_techer"
            android:parentActivityName=".First"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".teacher_login"
            android:parentActivityName=".First" />
        <activity
            android:name=".Std_login"
            android:parentActivityName=".First" />
        <activity android:name=".Term_conditions" >

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            </activity>
        <activity android:name=".tech_signup" />
        <activity android:name=".std_signup" />
        <activity
            android:name=".aqibchat.ui.activities.UserListingActivity"
            android:label="@string/app_name"
            android:parentActivityName=".navigation_bar"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".aqibchat.ui.activities.ChatActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".Upload_files" />
        <activity android:name=".Download_files" />
        <activity android:name=".Comments"></activity>
    </application>

</manifest>